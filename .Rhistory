one=c(1,2,3)
two=c("one", "two", "three")
names(one) = two
one
#vector functions
#1. selecting values
one[1:2] #vectors here are indexed from 1
one[c(1:2)]
one=c(1,2,3)
two=c("one", "two", "three")
names(one) = two
one
#vector functions
#1. selecting values
one[1:2] #vectors here are indexed from 1
one[c(1:3)]
one=c(1,2,3)
two=c("one", "two", "three")
names(one) = two
one
#vector functions
#1. selecting values
one[1:2] #vectors here are indexed from 1
one[c(1:3)]
one[-1]
one=c(1,2,3)
two=c("one", "two", "three")
names(one) = two
one
#vector functions
#1. selecting values
one[1:2] #vectors here are indexed from 1
one[c(1:3)]
one[-1]
one["one"]
one[c(1,3)]
one=c(1,2,3)
two=c("one", "two", "three")
names(one) = two
one
#vector functions
#1. selecting values
one[1:2] #vectors here are indexed from 1
one[c(1,3)]
one[-1]
one["one"]
one=c(1,2,3)
two=c("one", "two", "three")
names(one) = two
one
#vector functions
#1. selecting values
one[1:2] #vectors here are indexed from 1
one[c(1,3)]
one[-1]
one["one"]
one[one>1]
more functions
x=c(3,4,7)
x+3
z = c(1.5, 1/6, 1/3)
round(z, 2) #rounds each element in the matrix to 2 decimal places
sort(z)
length(z)
max(z)
mean(z)
fpe = read.table("http://data.princeton.edu/wws509/datasets/effort.dat")
fpe
fpe$setting
fpe[fpe$setting >= 40]
fpe[fpe$setting >= 40,]
fpe[fpe$setting >= 80 & fpe$effort < 10,]
boxplot(fpe$setting)
plot(fpe$setting, fpe$effort)
plot(fpe$setting, fpe$effort, pch=21)
plot(fpe$setting, fpe$effort, pch=21, bg-"gold")
plot(fpe$setting, fpe$effort, pch=21, bg="gold")
plot(fpe$setting, fpe$effort, bg="gold")
mazdas <- read.delim("C:\\Users\\SESHAD~1\\AppData\\Local\\Temp\\RtmpewtRmi\\data244c502f444")
View(mazdas)
summary(mazdas)
mazdas.head()
boxplot(mazdas$Year)
boxplot(mazdas$Price)
mazdas
mazdas.head
mazdas.head()
View(mazdas)
attach(mazdas)
Price
